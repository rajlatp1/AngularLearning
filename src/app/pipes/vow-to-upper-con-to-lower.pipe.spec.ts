import { VowToUpperConToLowerPipe } from './vow-to-upper-con-to-lower.pipe';

describe('VowToUpperConToLowerPipe', () => {
  it('create an instance', () => {
    const pipe = new VowToUpperConToLowerPipe();
    expect(pipe).toBeTruthy();
  });
});
